apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-demo.fullname" . }}
  labels:
    app: {{ include "argocd-demo.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "argocd-demo.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      nodePort: {{ .Values.service.nodePort }}
